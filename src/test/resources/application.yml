# Test
spring:
  profiles:
    active: local
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect

# local 환경
---
spring:
  profiles: local
  jpa:
    show-sql: true